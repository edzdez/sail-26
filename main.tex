\documentclass{beamer}
\usepackage{amsmath}
\usepackage{formal-grammar,proof,forest}
\usepackage{./prelude}
\usepackage[mathrm=sym]{unicode-math}

\setlength{\parskip}{\baselineskip}

\newcommand{\num}[1]{\texttt{num}[#1]}
\newcommand{\ctx}[1]{\langle #1 \rangle}
\newcommand{\set}[1]{\{ #1 \}}

\title{Template}
\author{Ethan Zhang, Srujan Roplekar}
\institute[]{}
\date{}

\begin{document}

\frame{\titlepage}

\section{Introduction}

\begin{frame}
	\frametitle{What are Semantics?}
\end{frame}

\begin{frame}
	\frametitle{Why Study Semantics?}
\end{frame}

\begin{frame}
	\tableofcontents
\end{frame}

\section{Arithmetic Expressions}

\begin{frame}
	\frametitle{Arithmetic Expressions: Syntax}

	\begin{center}
		\begin{grammar}
			\firstcase{e}{\num{n}}{Numeric constants}
			\otherform{\nonterm{e} \mathrel{\texttt{+}} \nonterm{e}}{Addition}
			\otherform{\nonterm{e} \mathrel{\texttt{*}} \nonterm{e}}{Multiplication}
		\end{grammar}
	\end{center}

	In English, an expression $e$ is:
	\vspace{-\parskip}
	\begin{itemize}
		\item a numeric constant \num{$n$} where $n$ is a number,
		\item a sum $e \mathrel{\texttt{+}} e$ where each $e$ is an expression, or
		\item a product $e \mathrel{\texttt{*}} e$ where each $e$ is an expression.
	\end{itemize}
\end{frame}

\begin{frame}
	\frametitle{Arithmetic Expressions: Example}

	\begin{columns}
		\begin{column}{0.45\textwidth}
			\centering
			\texttt{1 + ((2 + 3) * 4)}
		\end{column}
		\begin{column}{0.1\textwidth}
			\centering
			$\longmapsto$
		\end{column}
		\begin{column}{0.45\textwidth}
			\centering
			\begin{forest}
				[
					\texttt{+}
					[
						\num{1}
					]
					[
						\texttt{*}
						[
							\texttt{+}
							[
								\num{2}
							]
							[
								\num{3}
							]
						]
						[
							\num{4}
						]
					]
				]
			\end{forest}
		\end{column}
	\end{columns}
\end{frame}

\begin{frame}
	\frametitle{Aside: (Big-step) Operational Semantics}

	The goal of \emph{operational semantics} is to describe a program by \textbf{what it does}.

	But what does this mean?
	One idea is to think of programs as \emph{functions}:
	given some inputs, we get some output.

	Following this intuition, big-step semantics describes a program by its final result.
	For example, the program $\texttt{1 + ((2 + 3) * 4)}$ is described by the number $21$.
\end{frame}

\begin{frame}
	\frametitle{Aside: Inference Rules}

	As computer scientists, we want to describe a program's semantics by formal rules, so that we can both reason about them mathematically and implement them in code.

	Following the tradition of logic, semantics are described by \emph{inference rules}:
	\[
		\infer[(\textsc{Rule-Name})]
		{
			J
		}
		{
			J_1 & J_2 & \dots & J_n
		}
	\]
	In English, we say that ``$J$ holds, provided that $J_1$, $J_2$, $\dots$, $J_n$ all hold.''
\end{frame}

\begin{frame}
	\frametitle{Arithmetic Expressions: Semantics}

	% FIXME: Should we get rid of the (pause) shadows?
	\[
		\infer[(\textsc{Const})]
		{
			\num{n} \Downarrow \num{n}
		}
		{}
	\]
	\pause
	\vspace{.5em}
	\[
		\infer[(\textsc{Add})]
		{
			e_1 + e_2 \Downarrow \num{n + m}
		}
		{
			e_1 \Downarrow \num{n} & e_2 \Downarrow \num{m}
		}
		\qquad
		\pause
		\infer[(\textsc{Mul})]
		{
			e_1 * e_2 \Downarrow \num{n \cdot m}
		}
		{
			e_1 \Downarrow \num{n} & e_2 \Downarrow \num{m}
		}
	\]
\end{frame}

\section{Adding Variables}

\begin{frame}
	\frametitle{Adding Variables: Syntax}

	\centering
	\begin{grammar}
		\firstcase{e}{\num{n}}{Numeric constants}
		\otherform{x}{Variable reference}
		\otherform{\nonterm{e} + \nonterm{e}}{Addition}
		\otherform{\nonterm{e} * \nonterm{e}}{Multiplication}
		\otherform{\texttt{let $x$ = $\nonterm{e}$ in $\nonterm{e}$}}{Variable definition}
	\end{grammar}

	% TODO: examples
\end{frame}

\begin{frame}
	\frametitle{Adding Variables: Context}

	How do we keep track of the current value of a variable?
	By introducing an \emph{evaluation context}, which you can think of as a ``bag'' of data necessary to execute a program.

	In \textsc{Arith}, the context only contained the program to be executed.
	In \textsc{Arith+Var}, we must also keep track of a mapping of variable symbols to values, which we'll call the \emph{environment}:
	\[
		\ctx{ \text{program}, \text{environment} }
	\]
\end{frame}

\begin{frame}
	\frametitle{Adding Variables: Semantics}
	\[
		\infer[(\textsc{Const})]
		{
			\ctx{ \num{n}, S } \Downarrow \num{n}
		}
		{}
		\qquad
		\infer[(\textsc{Var})]
		{
			\ctx{ x, S } \Downarrow \num{n}
		}
		{
			x \mapsto \num{n} \text{ in } S
		}
	\]
	\vspace{.5em}
	\[
		\infer[(\textsc{Add})]
		{
			e_1 + e_2 \Downarrow \num{m + n}
		}
		{
			e_1 \Downarrow \num{m} & e_2 \Downarrow \num{n}
		}
		\qquad
		\infer[(\textsc{Mul})]
		{
			e_1 * e_2 \Downarrow \num{m \cdot n}
		}
		{
			e_1 \Downarrow \num{m} & e_2 \Downarrow \num{n}
		}
	\]
	\vspace{.5em}
	\[
		\infer[(\textsc{Let})]
		{
			\ctx{ \texttt{let $x$ = $e_1$ in $e_2$}, S } \Downarrow v
		}
		{
			\ctx{ e_1, S } \Downarrow v_1 & \ctx{ e_2, S\set{ x \mapsto v_1 } } \Downarrow v
		}
	\]
\end{frame}

\end{document}
